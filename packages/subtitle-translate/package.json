{
  "name": "@speedora/subtitle-translate",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless module (JSON-in/JSON-out) - translates a video's transcript segments into a target language via one batched LLM call. No DB/queue access at all; see root ARCHITECTURE.md. Consumed by apps/worker's translate-transcript adapter (Subtitle Studio roadmap, P2f).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "openai": "^6.45.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.9.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}

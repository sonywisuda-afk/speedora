{
  "name": "@speedora/multi-speaker-reasoning",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless module (JSON-in/JSON-out): AI Intelligence v4 Phase 6 - a pure, synchronous composition (no LLM call) that attributes Phase 4's MomentumCurve and Phase 5's EmotionalArc to individual speakers via Speaker Intelligence's SpeakerTimelineEntry[], producing a per-speaker breakdown. Null for the majority single-speaker case, by design. See docs/ai/intelligence-v4.md. No DB/queue access at all; see root ARCHITECTURE.md. Consumed by apps/worker's render-graph multi-speaker-reasoning node.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@speedora/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^22.20.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.9.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}

{
  "name": "@speedora/edit-plan-director",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless module (JSON-in/JSON-out): Edit Plan Director Phase B (\"Edit Orchestration\" - see docs/ai/edit-plan-director.md). A pure, synchronous composition (no LLM call) over the Visual Emphasis Engine's own EditingSuggestion[] timeline - an adaptive per-technique Edit Budget plus an Effect Conflict Resolver implementing the specific pairwise rules grounded in docs/ai/visual-emphasis-integration-audit.md's Gate B real-render evidence (Focus Shift x Digital Push, OCR Highlight x moving crop, Reaction Hold x Pause Hold). No DB/queue access at all; see root ARCHITECTURE.md. Consumed by apps/worker's render-clip.worker.ts.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@speedora/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^22.20.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.9.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}

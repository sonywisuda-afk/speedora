{
  "name": "@speedora/speaker-fusion",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless module (JSON-in/JSON-out): Speaker Intelligence Phase F (Cross-module Fusion) - a pure, synchronous composition (no LLM call, no new detector) reshaping Phase C's Conversation Dynamics, the existing @speedora/speaker-scoring SpeakerFusionFeatures, and Phase E's speaker_focus_shift editing suggestions into one structured, queryable FinalSpeakerIntelligence read-model. Deliberately NOT a numeric re-fusion - no top-level composite score, no touching @speedora/fusion-engine's weights.ts. See docs/ai/speaker-intelligence.md. No DB/queue access at all; see root ARCHITECTURE.md. Consumed by apps/worker's render-graph finalSpeakerIntelligence node.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@speedora/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^22.20.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.9.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}

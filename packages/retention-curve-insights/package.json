{
  "name": "@speedora/retention-curve-insights",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless module (JSON-in/JSON-out): AI Intelligence v4 Phase 10 - a pure, synchronous composition (no LLM call) that derives Expected Drop Points, Replay Zones, Emotional Peaks, and Curiosity Peaks (spec Part 5, Retention Curve Prediction) from Phase 4's MomentumCurve, Phase 5's EmotionalArc, and Phase 2's SemanticEvent[] - a new additive layer, MomentumCurve/EmotionalArc themselves are unchanged. See docs/ai/intelligence-v4.md. No DB/queue access at all; see root ARCHITECTURE.md. Consumed by apps/worker's render-graph retentionCurveInsights node.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@speedora/contracts": "workspace:*",
    "@speedora/scene-intelligence": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^22.20.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.9.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}

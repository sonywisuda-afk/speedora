{
  "name": "@speedora/clip-query-parser",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless module (JSON-in/JSON-out) - parses a free-text search query into structured Clip Library filter params via one LLM call. No DB/queue access at all; see root ARCHITECTURE.md. Consumed synchronously by apps/api's ClipQueryParserService (Natural Language AI Search roadmap, P4).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@speedora/shared": "workspace:*",
    "openai": "^6.45.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.5"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}

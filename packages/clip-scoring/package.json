{
  "name": "@speedora/clip-scoring",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless clip-scoring module (JSON-in/JSON-out) - picks and scores viral-clip candidates from a transcript via an LLM call. No DB/queue access at all; see root ARCHITECTURE.md and packages/contracts for the pattern this follows. Consumed by apps/worker's detect-clips adapter.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@speedora/contracts": "workspace:*",
    "@speedora/shared": "workspace:*",
    "openai": "^6.45.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.5"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}

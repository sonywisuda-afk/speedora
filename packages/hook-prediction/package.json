{
  "name": "@speedora/hook-prediction",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless module (JSON-in/JSON-out): AI Intelligence v4 Phase 1 - predicts whether a clip's opening hooks a scrolling viewer, combining already-computed signals (AudioFeatures, dominant speaker confidence, a reused cutlist silence-gap pause feature) with one new LLM reasoning step for sentiment/surprise/controversy/keyword rarity/topic shift/question density/numeric facts/named entities. See docs/ai/intelligence-v4.md. No DB/queue access at all; see root ARCHITECTURE.md. Consumed by apps/worker's render-graph hook-prediction node.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@speedora/contracts": "workspace:*",
    "@speedora/cutlist": "workspace:*",
    "@speedora/llm-client": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "openai": "^6.45.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.9.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}
